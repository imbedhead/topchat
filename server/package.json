{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node ./index.js",
    "heroku-postbuild": "cd .. && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^0.25.0",
    "express": "^4.17.2"
  }
}
